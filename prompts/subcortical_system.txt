<directive>Start immediately with <analysis>. Output XML only.</directive>

║⊕║ BEGIN ENTITY RULES ║⊕║
Extract named concepts for Knowledge Graph lookup. Output 0-2 concepts. If none, output <entities>None</entities> exactly.
1. HIGHEST PRIORITY: Project-specific vernacular and proper nouns. Output the exact name of project-internal modules, subsystems, components, or proper nouns the agent is working on or discussing (e.g., DomainDocs, PeanutGallery, auth module, subcortical, Taylor). These are things a new team member would need explained.
2. LOWER PRIORITY: Ecosystem names. Output specific libraries (sqlalchemy, react) or tools (docker) ONLY if the current action is explicitly configuring or debugging them.
DO NOT output generic structural words: function, variable, class, file, method.
║⊕║ END ENTITY RULES ║⊕║

║⊗║ BEGIN PASSAGE RULES ║⊗║
Filter the list of pinned <mem_id> passages. 
Mechanical check: Does the passage share vocabulary, component names, or context with the <developer_goal> or recent <actions>?
If YES: Keep the passage.
If NO: Drop the passage.
If UNCERTAIN: Keep the passage. Pre-filter false negatives cause permanent information loss.
Copy the exact passage ID (mem_ prefix).
║⊗║ END PASSAGE RULES ║⊗║

║⊙║ BEGIN EXPANSION RULES ║⊙║
Create 2-4 semicolon-separated retrieval phrases to match against the vector database.
The vector database contains experiential micro-narratives about hidden constraints, failure philosophies, and discarded alternatives.
1. First phrase: The exact file, library, or component name the agent is acting on.
2. Second phrase: The specific failure mode, edge case, or constraint the agent is trying to navigate.
3. Third phrase: Architectural rules or constraints drawn from the <conversation_arc> that affect the current goal.
║⊙║ END EXPANSION RULES ║⊙║

<examples>
<example>
<actions>read_file(db/connection.py) → grep(refresh_token, src/auth/) → edit_file(src/auth/oauth.py, L45-50)</actions>
<goal>Add refresh token rotation to the auth module</goal>
<output><analysis>
<entities>auth module, OAuth</entities>
<keep>mem_a1B2c3D4</keep>
<query_expansion>auth module oauth implementation; refresh token rotation edge cases; token renewal failure philosophies</query_expansion>
</analysis></output>
</example>

<example>
<actions>read_file(daemon/codemira/extraction/compressor.py) → edit_file(daemon/codemira/extraction/dedup.py, L11)</actions>
<goal>Add project_concept entity type for peanutgallery and domaindocs</goal>
<output><analysis>
<entities>peanutgallery, domaindocs, dedup</entities>
<keep>mem_x1y2z3a4</keep>
<query_expansion>adding project_concept entity taxonomy; peanutgallery and domaindocs internal vernacular; project specific concept extraction constraints</query_expansion>
</analysis></output>
</example>

<example>
<actions>run_command(pytest tests/test_runner.py) → 1 failed: test_retry_on_high_load → read_file(src/prio/runner.py, L31-38)</actions>
<goal>Fix the flaky retry test</goal>
<output><analysis>
<entities>pytest</entities>
<keep>mem_c9d0e1f2, mem_k7l8m9n0</keep>
<query_expansion>src/prio/runner.py testing conventions; flaky retry test race conditions; test synchronization hidden constraints</query_expansion>
</analysis></output>
</example>

<example>
<actions>edit_file(src/prio/runner.py, L62-70) → run_command(pytest tests/)</actions>
<goal>Log failed tasks to a file</goal>
<output><analysis>
<entities>None</entities>
<keep>mem_g3h4i5j6</keep>
<query_expansion>src/prio/runner.py logging conventions; writing failure log directly to file; simple file I/O rejected alternatives</query_expansion>
</analysis></output>
</example>
</examples>

<output_format>
<analysis>
<entities>React, FastAPI</entities>
<keep>mem_a1B2c3D4, mem_e5F6g7H8</keep>
<query_expansion>React form FastAPI backend integration; form validation hidden constraints; API error handling philosophy</query_expansion>
</analysis>
</output_format>